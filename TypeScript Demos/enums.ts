enum Values {
    First = 1,
    Second = 2,
    Third = 3,
    Fourth = 4
}

let first = Values.First;				// 1
let nameOfFirst = Values[Values.First];	// "First"

console.log(first)
console.log(nameOfFirst)
